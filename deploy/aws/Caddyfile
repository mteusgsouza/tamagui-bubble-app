# O Caddy emite e renova o certificado sozinho; o único requisito é que o nome já
# resolva para esta máquina antes de subir.
#
# ℹ️ Havia um segundo bloco aqui, `zero.` → `zero:4848`, que saiu com o zero-cache.
# O registro de DNS correspondente pode ser apagado.

bubble.mateusgsouza.com.br {
	reverse_proxy app:8092
}

