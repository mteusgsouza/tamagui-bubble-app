node_modules
test-results

ios/**
android/**

packages/*/types

tmp-data

# keep types in repo to track them
# **/types/**/*.d.ts
# but map can be ignored and just published on npm
**/types/**/*.d.ts.map

tmp
dist
.DS_Store
.ultra.cache.json
*.tsbuildinfo

.build
.tamagui

# generated claude code skills (run `bun tko docs skills` to regenerate)
.claude/skills/takeout-*

.env
.env.production

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.backup
.env.ios

.expo

# test output
.output

# Logs
.logs/

.watchman*

# Docker volume data
.docker
.vxrn-entry-native.tsx

# permissões locais do Claude Code — máquina de quem roda, não do projeto
.claude/settings.local.json

# saída do build para a Vercel (Build Output API) e o link do projeto pelo CLI
.vercel

# envs preenchidos do deploy (os *.example continuam versionados)
deploy/**/app.env
deploy/**/zero.env
scripts/deploy.sh
